hazelcast:
  cluster-name: bytestree-hazelcast-cluster
  map:
    products:
      max-idle-seconds: 3600
      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 300
  network:
    join:
      multicast:
        enabled: true